import {createMuiTheme} from '@material-ui/core';

const appTheme = createMuiTheme({
  palette: {
    primary: {
      main: '#E77728',
    },
    secondary: {
      main: '#546A76',
    },
    accent: {
      main: '#D05353',
    },
  },
});

export default appTheme;
